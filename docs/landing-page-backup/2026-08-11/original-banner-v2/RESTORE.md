# Original banner v2 restore point

This animation was added only to `app/components/header.tsx` and
`app/styles/app.css`. The untouched source remains at
`public/brand/banner/source/background3-original.jpg`.

The previous restored banner files remain available in
`docs/landing-page-backup/2026-08-11/animated-banner-prototype/`.

The v2 implementation uses only the original-derived files under
`public/brand/banner/animation-svg/`; it does not use any file under
`reference-elements-*`.
